import { View } from 'react-native';

// This is just a placeholder screen for the add button
// The actual functionality is handled by the tab button press
export default function AddScreen() {
  return <View />;
} 